{
  "name": "travis-intro-node",
  "version": "1.0.0",
  "description": "A sample app to start a web server",
  "main": "lib/app.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "start": "node -e 'app = require(./lib/app); app.start()'",
    "test": "node ./node_modules/mocha/bin/mocha"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^3.0"
  }
}
